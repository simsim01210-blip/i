# 테스트 방법

## Minecraft 1.20.1

1. Fabric Loader 1.20.1을 설치합니다.
2. Fabric API 1.20.1을 `mods` 폴더에 넣습니다.
3. `build/libs/planetmap-mc1.20.1-1.0.11.jar`도 `mods` 폴더에 넣습니다.
4. 게임에서 PlanetEarth 서버에 접속합니다.
5. `M` 키를 눌러 편집 화면을 엽니다.

확인할 항목:

- 미니맵을 드래그하면 위치가 바뀌는지
- 오른쪽 아래 흰색 핸들을 드래그하면 크기가 바뀌는지
- 지도, 그리드, 플레이어 버튼이 각각 켜지고 꺼지는지
- 그리드 선이 고정 화면 간격이 아니라 월드 X/Z 16블록 단위의 실제 청크 경계를 따라 움직이는지
- 지도 타일이 하나도 로딩되지 않았을 때 제공된 정사각형 얼굴이 지도 크기에 비례해 작게 표시되고 내부 테두리에서 반사 이동하는지
- 로딩 배경에 새 1254×1254 PlanetMap 이미지가 원본 비율로 표시되고 남는 공간이 청록·초록·파랑 그라데이션으로 채워지는지
- 로고와 움직이는 얼굴이 별도 렌더링 깊이에서 완전히 표시되고 얼굴이 정확한 1:1 비율인지
- 움직이는 얼굴이 크롭·누끼·외곽선 없이 제공된 600x600 원본의 흰 여백까지 모두 표시되는지
- 플레이어 이동 중 타일 판정이 바뀌어도 로딩 화면이 깜빡이거나 얼굴 위치가 순간 이동하지 않는지
- 얼굴이 절대시간이 아닌 프레임 누적 방식으로 천천히 연속 이동하는지
- 움직이는 얼굴 좌표를 정수로 반올림하지 않아 한 픽셀씩 덜덜 떨지 않고 부드럽게 이동하는지
- 로딩 여백이 단순 수평 3색이 아닌 청록·초록·파랑·남색의 대각선 색 흐름과 가장자리 음영으로 표시되는지
- 현재 화면에 필요한 타일 중 하나라도 빠졌으면 로딩 배경이 유지되고 모든 타일이 준비된 뒤 한 번에 지도로 바뀌는지
- 서버가 HTTP 200으로 반환한 256바이트 이하의 빈 WebP 타일도 미로딩 상태로 판정하는지
- 복잡한 Towny 영역이 거대한 삼각형 없이 내부를 반투명하게 채우는지
- 영역과 사이트 마커가 영역 정보창 위를 가리지 않는지
- 타일 1초, 플레이어 0.5초, 영역·사이트 마커 2초 주기로 빠르게 갱신되는지
- 한글 검색 중 검색창 인스턴스와 IME 조합 상태가 유지되어 글자가 누락되거나 건너뛰지 않는지
- 저장 후 재접속해도 설정이 유지되는지
- PlanetEarth 지도 타일이 현재 좌표를 중심으로 표시되는지
- 타일 경계에서 도로와 건물이 끊기거나 다른 지역과 섞이지 않는지
- 노란 화살표와 북/북동/동/남동/남/남서/서/북서 방향 글자가 시점에 맞게 바뀌는지
- 웹지도에 공개된 주변 온라인 플레이어가 얼굴 이미지와 닉네임으로 표시되는지
- 얼굴 전체가 잘리지 않고 축소 표시되는지
- M 편집 화면에서 얼굴과 닉네임을 각각 끄거나 크기를 바꿀 수 있는지
- 얼굴, 닉네임, 내 위치와 지도 배율을 슬라이더로 조절할 수 있는지
- 지도 크기 슬라이더 없이 왼쪽, 오른쪽, 위, 아래와 네 모서리를 드래그해 크기를 바꿀 수 있는지
- 좌우 경계를 움직이면 가로만, 위아래 경계를 움직이면 세로만 변경되는지
- N 키로 전체 지도가 열리고 드래그 이동과 휠 확대/축소가 가능한지
- N 키를 다시 누르면 전체 지도가 닫히고 M 키를 다시 누르면 편집 화면이 닫히는지
- 미니맵에도 웹지도 색상의 국가·마을 영토 내부 채움과 경계가 표시되는지
- M 화면에서 웨이포인트 표시를 끄고 켤 수 있는지
- N 화면에서 다른 플레이어 표시를 끄고 켤 수 있는지
- N 화면의 온라인 플레이어 검색에서 한글·영문 닉네임 일부를 입력해 필터링되는지
- 마커·플레이어 검색창에 영문 N을 입력해도 전체 지도가 닫히지 않고 문자로 입력되는지
- 검색 결과를 누르면 이동 중인 플레이어의 검색 당시 좌표가 아니라 가장 최근 좌표로 지도가 이동하는지
- 저장한 웨이포인트가 화면에 신호기형 기둥과 이름·거리로 표시되는지
- 웨이포인트 이름과 거리가 미니맵 내부가 아니라 실제 게임 화면에 표시되고 벽 뒤에서도 유지되는지
- 신호기 기둥은 실제 목표 좌표에서 하늘로 이어지고 기둥 자체는 블록을 관통하지 않는지
- 신호기 기둥이 목표보다 64블록 위에 떠 있지 않고 실제 저장 Y 좌표에서 하늘까지 이어지는지
- 기둥 아래쪽에 느낌표처럼 보이는 사각형 마감이 없는지
- 이름과 거리가 기둥 꼭대기가 아니라 실제 목표 지점 옆에 표시되는지
- 망원경이나 줌 모드로 확대할 때 기둥의 화면 위치와 길이가 월드 화면과 함께 확대되는지
- 기둥 굵기도 거리와 현재 FOV에 따라 변해 확대 전후 원근 비율이 유지되는지
- 오른쪽 웨이포인트 목록을 눌렀을 때 지도만 이동하고 길 안내는 시작되지 않는지
- 오른쪽 사이트 마커 검색 결과를 눌렀을 때도 지도만 이동하고 길 안내는 시작되지 않는지
- 지도 위의 웨이포인트 핀이나 사이트 마커를 직접 좌클릭했을 때만 길 안내가 시작되는지
- 사이트 마커나 웨이포인트를 좌클릭하면 미니맵에 목표 방향 점선과 이름·거리가 표시되는지
- 길 안내 점선이 노란색이 아니라 흰색으로 표시되는지
- 경유지 A, B, C를 차례로 추가하면 `C → B → A → 최종 목적지` 순서로 안내되는지
- N 전체 지도의 경유지 번호가 위 실제 방문 순서대로 즉시 다시 매겨지는지
- 목표 5블록 이내에 도착하면 플레이어 레벨업 효과음이 한 번 재생되고 길 안내가 종료되는지
- 전체 지도에서 우클릭해도 화면이 바뀌지 않고 클릭 지점 위의 지정 메뉴로 웨이포인트를 저장할 수 있는지
- 직접 지정한 웨이포인트가 투명 배경의 빨간 위치 핀 아이콘으로 표시되고 핀 끝이 정확한 좌표를 가리키는지
- 지정 메뉴를 누르면 해당 지점 바로 위에 이름 입력창과 저장·취소 버튼이 나타나는지
- 오른쪽 웨이포인트 목록의 삭제 버튼이 해당 웨이포인트만 즉시 삭제하는지
- 새 웨이포인트 창에서 핀·작은 원·머리 모양을 선택하고 저장할 수 있는지
- N 오른쪽 목록의 모양 버튼으로 기존 웨이포인트 모양을 개별 변경할 수 있는지
- 작은 원의 지름이 핀보다 작고 선택 색은 원 테두리에만 적용되는지
- 머리 모양이 크롭·누끼 없이 사각형 전체로 표시되며 흰 부분만 선택 색으로 바뀌는지
- 오른쪽 웨이포인트 버튼을 누르면 해당 좌표로 자동 이동하는지
- 기차역, Towny, 항구, 특산품, 운하, TownyKOTH 마커와 원본 아이콘이 표시되는지
- 사이트 마커 카테고리를 누르면 켜짐·꺼짐 대신 하위 마커 목록으로 이동하는지
- 하위 마커 목록을 휠로 스크롤하고 항목을 누르면 해당 좌표로 지도가 이동하는지
- 기차역, Towny, 항구, 특산품, 운하, TownyKOTH 하위 목록에서 검색어 입력 즉시 이름이 필터링되는지
- 하위 목록의 뒤로가기 버튼으로 카테고리 목록에 복귀하는지
- Towny 등 나라 영토 내부가 웹지도에서 제공하는 fillcolor와 fillopacity 0.35로 표시되는지
- 멀리 축소해도 여러 영토 띠가 같은 화면 픽셀에 반복 합성되어 겹겹이 진해지지 않는지
- 전체 지도의 내 위치가 스킨 얼굴과 닉네임으로 표시되고 금색 테두리·별표·나 표기로 돋보이는지
- 화면 픽셀 단위로 병합한 스캔라인이 테두리뿐 아니라 내부 채움도 실제로 출력하는지
- 영역을 좌클릭하면 국가, 마을과 상세 정보가 지도 위에 표시되는지
- 사이트 마커와 내 웨이포인트를 각각 켜고 끄거나 크기를 조절할 수 있는지
- 확대·축소에 따라 사이트 마커와 내 웨이포인트 크기가 함께 변하는지
- 버튼과 슬라이더에서 작은 entity.generic.eat 소리가 피치 2.0으로 재생되는지
- 설정을 열고 닫을 때 작은 entity.experience_orb.pickup 소리가 피치 0.6으로 재생되는지
- 효과음 전체를 끄거나 음량 슬라이더로 조절할 수 있는지
- 내 위치 화살표가 흰색과 검은색 테두리로 표시되고 크기를 바꿀 수 있는지
- LiveAtlas 업데이트 피드가 변경된 타일을 감지하면 다음 렌더 요청에서 갱신되는지

설정 파일은 `.minecraft/config/planetearth-minimap.json`에 저장됩니다.

Minecraft 버전에 맞는 별도 JAR을 사용합니다. 1.20.1~1.20.4는 Java 17, 1.20.5~1.21.11은 Java 21이 필요합니다.

## 현재 검증 결과

- Minecraft 1.20.1 Java 컴파일과 정식 JAR 빌드: 성공
- Minecraft 1.20.1~1.21.11 정식 18개 버전 전체 재빌드: 성공
- 18개 JAR의 `fabric.mod.json` 버전 `1.0.11`과 정확한 Minecraft `~버전` 의존성 검사: 성공
- 18개 JAR의 색상 핀·색상 저장·생성 UI·HUD·미니맵 바이트코드 검사: 성공
- 18개 JAR에서 신호기 레이저·레이캐스트 코드 제거 검사: 성공
- 원하는 지점 클릭 후 길안내 버튼 표시·즉시 시작 코드 제거·클릭/드래그 구분 검사: 성공
- N 전체 지도 목적지 상태·흰 점선·최종 목적지/경유지 분리 코드 검사: 성공
- 경유지 순서 목록·추가·삭제·도착 전환과 최종 목적지 취소 분기 검사: 성공
- 18개 JAR에서 새 경유지를 목록 맨 앞에 삽입하는 후입선출 바이트코드 검사: 성공
- 영토 병합 캐시·직선 경계선 최적화·타일 순서 캐시·변경 없는 마커 응답 생략 코드 검사: 성공
- 플레이어 검색·최신 위치 재조회·접속자 명단 리비전 분리 코드 검사: 성공
- 영토 오버스캔 캐시·경계선 캐시·타일 업로드 프레임 예산·마커 X 범위 검색 코드 검사: 성공
- 새 PlanetMap 로딩 원본·무크롭 머리 원본·세 가지 웨이포인트 모양·색상 머리 텍스처 코드 검사: 성공
- 검색창 포커스 중 N 단축키 억제 코드 검사: 성공
- 1.20.1~1.21.1의 `double` FOV 및 1.21.2~1.21.11의 `float` FOV 접근 확장 연결 검사: 성공
- 18개 JAR SHA-256 재계산과 전체 버전 ZIP 생성: 성공
- 실제 1.20.1 로그에서 수정 전 얼굴 텍스처 누락 93회와 OpenGL 오류 93회 확인
- 수정 후 실제 PlanetEarth 서버 재접속 렌더링 검사: 게임 실행 중이라 새 JAR 재시작 대기
- Fabric Loader 0.16.14 개발 클라이언트 기동: 성공
- Minecraft 1.20.1 모드 초기화, 새 PlanetMap 아이콘과 리소스 로딩: 성공
- 실제 PlanetEarth 서버 접속 테스트: 개발 클라이언트가 Microsoft 인증 계정을 사용하지 않아 미실시
