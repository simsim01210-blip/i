# 변경 기록

## 1.0.14
- 웨이포인트 모양 선택에서 작은 원과 플레이어 머리를 제거하고 핀으로 통일
- 기존 설정에 저장된 원/머리 웨이포인트는 불러올 때 자동으로 핀으로 변환
- M 설정과 N 전체 지도에서 불필요해진 웨이포인트 모양 선택 UI 제거
- 모드 내장 아이콘을 투명 배경 PlanetMap 로고로 교체
- 배포 파일명을 `planetmap-[마인크래프트 버전]-lil0ri.jar` 형식으로 변경

## 1.0.13

- Removed the black canvas surrounding the PlanetMap unloaded-map fallback logo.
- Preserved the logo text, colors, internal black outlines, and pale blue outer border while adding transparent padding.

## 1.0.11

- 미니맵 타일 미로딩 화면의 기존 로고를 첨부된 1254×1254 PlanetMap 이미지 원본으로 교체
- 튕기는 머리는 첨부된 600×600 PNG의 흰 여백을 포함한 원본을 크롭·누끼 처리 없이 유지
- 웨이포인트별로 `핀`, 지름을 줄인 `작은 원`, `머리` 모양 선택 및 설정 저장 지원
- 머리 모양은 원본 사각형 구성을 유지하면서 흰 부분만 웨이포인트 색으로 변경
- 작은 원 모양은 내부가 빈 원 테두리에 웨이포인트 색 적용
- M 편집 화면에서 새 웨이포인트 기본 모양 변경, N 목록에서 기존 웨이포인트 모양 개별 변경
- 마커·플레이어 검색창에 포커스가 있을 때 N 키 입력으로 전체 지도가 닫히던 문제 수정

## 1.0.10

- N 전체 지도 오른쪽 메뉴에 온라인 플레이어 검색 목록 추가
- 한글·영문 닉네임 일부 검색과 검색 결과 클릭 시 해당 플레이어의 최신 위치로 지도 이동
- 플레이어 좌표가 0.5초마다 바뀌어도 검색 UI를 다시 만들지 않도록 접속자 명단과 실시간 좌표 갱신 분리
- 영토 내부와 경계선을 넓은 화면 범위로 미리 캐시해 지도 드래그 1픽셀마다 도형을 다시 계산하던 병목 제거
- 동시에 완료된 지도 타일의 GPU 업로드를 여러 프레임으로 분산하고 현재 화면 타일을 우선 처리해 전체 지도 진입·이동 시 순간 멈춤 완화
- 화면 X 범위 밖 사이트 마커를 이진 검색으로 건너뛰고, 최원거리 배율의 완전히 겹친 아이콘은 한 번만 렌더링

## 1.0.9

- 경유지를 추가한 순서가 아닌 마지막으로 추가한 경유지부터 방문하도록 변경
- 경로 순서를 `최신 경유지 → 이전 경유지들 → 최종 목적지`로 적용
- N 전체 지도의 경유지 번호도 실제 방문 순서를 기준으로 즉시 다시 표시

## 1.0.8

- 경유지가 현재 목적지를 교체하던 잘못된 구조를 최종 목적지와 경유지 목록으로 완전히 분리
- 경유지는 추가한 순서대로 먼저 방문하고 최종 목적지는 끝까지 유지
- 안내 중 빈 지점 선택 시 `경유지 추가`, 등록된 경유지 선택 시 `경유지 삭제` 버튼 표시
- 최종 목적지 선택 시에만 `길안내 취소` 버튼 표시
- N 전체 지도에서 경유지는 주황색 번호 마커, 최종 목적지는 노란색 마커로 구분
- 미니맵에 다음 경유지 또는 최종 목적지를 명시하고 경유지 도착 후 다음 구간으로 자동 전환
- 최종 목적지 도착 시에만 레벨업 효과음과 함께 길 안내 종료

## 1.0.7

- N 전체 지도에서 웨이포인트뿐 아니라 원하는 임의 지점을 목적지로 선택 가능
- 지도 클릭만으로 안내하지 않고 클릭 위치 위의 `길안내 시작` 버튼을 눌러야 시작하도록 변경
- 현재 목적지를 다시 선택하면 `길안내 취소` 버튼 표시
- 길 안내 중 다른 위치를 선택하면 `경유지 추가` 버튼으로 기존 목적지 앞에 경유지 삽입
- 클릭과 드래그를 구분해 지도 이동 중에는 목적지 선택 창이 열리지 않도록 수정
- N 전체 지도에 현재 목적지 이름·좌표·거리·남은 목적지 수와 흰 점선 경로 표시

## 1.0.6

- 신호기 레이저 기둥을 제거하고 실제 월드 위치에 지도 핀 아이콘으로 표시
- 새 웨이포인트 생성 창에 12색 팔레트 추가 및 웨이포인트별 색상 저장
- 기존 웨이포인트도 전체 지도 오른쪽 색상 버튼으로 개별 색상 변경 가능
- 색상 핀을 미니맵·전체 지도·게임 HUD에 동일하게 표시
- 전체 지도의 영토 채우기 결과를 화면 위치별로 캐시하고 인접 줄을 큰 사각형으로 병합
- 직선 영토 경계선을 픽셀 단위가 아닌 한 번의 사각형 그리기로 최적화
- 타일 정렬 결과를 화면 범위별로 재사용하고 변경 없는 마커 JSON 재분석 방지

## 1.0.5

- 망원경·줌 모드 사용 시 웨이포인트 기둥이 기본 FOV 비율에 고정되던 문제 수정
- 게임 렌더러가 실제로 적용 중인 현재 FOV로 웨이포인트 월드 좌표를 투영하도록 변경
- 기둥 굵기를 목표까지의 깊이와 현재 FOV에 맞춰 원근 비율로 조절
- 1.20.1~1.21.1의 `double` FOV와 1.21.2~1.21.11의 `float` FOV를 위한 버전별 접근 확장 분리

## 1.0.4

- 웨이포인트 신호기 기둥이 목표보다 64블록 위에서 시작해 공중에 떠 보이던 문제 수정
- 기둥을 저장된 목표 Y 좌표에서 시작해 하늘까지 이어지도록 변경
- 기둥 아래쪽 사각형 마감을 제거해 느낌표처럼 보이던 문제 수정
- 이름·거리 HUD를 기둥 꼭대기가 아니라 실제 목표 지점 옆에 배치

## 1.0.3

- 웨이포인트 이름과 거리를 미니맵이 아닌 실제 게임 화면 HUD에 표시하고 벽 뒤에서도 읽을 수 있도록 변경
- 웨이포인트 신호기 기둥은 목표 좌표의 지상 구간 대신 고정된 하늘 구간에 표시하고, 기둥 자체는 블록을 관통하지 않도록 유지
- 전체 지도 오른쪽 웨이포인트·사이트 마커 목록은 지도 이동만 수행하며 길 안내를 자동 시작하지 않도록 수정
- 지도 위 웨이포인트 핀이나 사이트 마커를 직접 좌클릭했을 때만 길 안내 시작
- 미니맵 길 안내 점선을 흰색으로 변경
- 게임 화면 웨이포인트가 많을 때 가까운 순서로 최대 8개 표시

## 1.0.2

- 플레이어 얼굴 HTTP 요청이 끝난 직후 중복 업로드가 예약되어 새 동적 텍스처를 다시 삭제하던 문제 수정
- 얼굴 누락 예외와 OpenGL 오류가 반복되어 게임이 불안정해지던 문제 수정
- 멀리 축소했을 때 여러 영토 띠가 같은 화면 픽셀에 반복 합성되어 영역이 겹겹이 진해지던 문제 수정
- 영토 내부 채움에 PlanetEarth 웹 지도의 원본 `fillopacity` 0.35 적용
- 화면 밖의 긴 영토 경계선을 먼저 잘라 불필요한 렌더링 반복과 프레임 저하 방지
- 타일·마커·얼굴 요청을 실제 GPU 텍스처 업로드 완료 시점까지 대기 상태로 유지해 중복 다운로드 방지

## 1.0.1

- 실시간 지도 타일을 갱신할 때 새 텍스처와 같은 ID를 가진 이전 텍스처를 뒤늦게 삭제해 검정·마젠타 누락 텍스처가 표시되던 문제 수정
- 웨이포인트 기둥을 고정된 3D 월드 좌표로 투영하고 블록 시야 검사를 적용해 이동 시 화면에 붙어 보이거나 벽을 관통하던 문제 수정

## 1.0.0

- 미니맵과 전체 지도에 LiveAtlas 국가·마을 영토 내부 채움 및 경계 표시
- 실시간 웹지도 타일, 업데이트 피드 무효화, 중앙 우선·주변 선행 로딩
- 온라인 플레이어 얼굴·닉네임과 현재 플레이어 강조 표시
- 청크 단위 그리드, 방향 화살표와 8방향 안내
- 미니맵 자유 이동과 가로·세로 독립 크기 조절
- 마커 카테고리, 검색, 클릭 이동과 영역 정보 팝업
- 웨이포인트 생성·이름 지정·이동·삭제 및 표시 크기 설정
- 웨이포인트 신호기형 HUD 기둥, 이름·거리와 M 화면 표시 토글
- 사이트 마커·웨이포인트 좌클릭 길 안내, 미니맵 점선과 도착 레벨업 효과음
- N 전체 지도 다른 플레이어 표시 토글
- 한글 IME 검색 입력 안정화
- 로딩 배경, 튕기는 얼굴 표시와 이동 떨림 완화
- UI 효과음 켜기·끄기와 음량 설정
- `M` 편집 화면과 `N` 전체 지도를 같은 키로 다시 닫는 토글 동작
- Minecraft 1.20.1~1.21.11용 버전별 호환 계층 추가
